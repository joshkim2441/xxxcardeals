Updates the product info based on ID
    ---
    tags:
      - Products
    parameters:
      - name: product_id
        in: path
        type: string
        required: true
        description: the unique id of the product
      - name: request
        in: body
        required: true

    responses:
      404:
        description: Product not found
      200:
        description: Request executed successfully
